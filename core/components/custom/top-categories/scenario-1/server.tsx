import { TopCategories } from './top-categories';

/**
 * This wrapper server component simply lets us deal with what typically
 * happens at the page route level (or other "outer" context) in isolation.
 *  - For now, just render the `<TopCategories>` component.
 */
export async function TopCategoriesExample() {
  return <>
    
  </>;
}
